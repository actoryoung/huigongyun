# Runtime dependencies for the MVP roadmap.
#
# Core dependencies (required):
pandas>=2.2.0
openpyxl>=3.1.5
rapidfuzz>=3.9.0
pdfplumber>=0.11.4
pydantic>=2.7.0
Flask>=3.0.0
celery>=5.2.7
redis>=4.5.0
minio>=7.1.1
psycopg2-binary>=2.9.7
tenacity>=8.2.2

# PDF OCR — traditional (optional, system deps: tesseract-ocr, poppler-utils)
pytesseract
pdf2image
Pillow

# PDF OCR — Vision LLM (optional, requires API key)
#   Set VISION_LLM_PROVIDER + *_API_KEY env vars; module lazy-loads at runtime.
# openai          # GPT-4o / GPT-4o-mini
# anthropic       # Claude Sonnet / Opus
# google-generativeai  # Gemini Flash / Pro

# PDF OCR — Marker local ML (optional, ~5 GB model download on first use, CPU only)
#   pip install marker-pdf  (pulls torch, transformers, surya-ocr as transitive deps)
# marker-pdf
