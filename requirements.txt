# Runtime dependencies for the MVP roadmap.
#
# Current scaffold can run with the Python standard library only.
# These packages are added for the next implementation steps:
# - Excel parsing and data shaping
# - PDF/table extraction
# - Material normalization and fuzzy matching
# - Lightweight web demo shell

pandas>=2.2.0
openpyxl>=3.1.5
rapidfuzz>=3.9.0
pdfplumber>=0.11.4
pydantic>=2.7.0
Flask>=3.0.0
celery>=5.2.7
redis>=4.5.0
minio>=7.1.1
psycopg2-binary>=2.9.7
